import { prisma } from "../db/prisma.js";

class TransactionModel {
  // approveUniversityPendingChange will be added in Phase 3
}

const transactionModel = new TransactionModel();

export { transactionModel };
